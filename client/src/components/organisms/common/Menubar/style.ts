import styled from 'styled-components';

export const MenubarContainer = styled.div`
	position: relative;
	width: 100%;
	height: 100%;
	animation: appear-slide-y 0.3s;
	display: flex;
	flex-direction: row;
	justify-content: center;
	align-items: center;
	gap: 2rem;
`;

import styled from 'styled-components';

export const ArticleFindPageLayoutContainer = styled.div`
	margin-top: 3rem;
	display: flex;
	flex-direction: column;
	gap: 10rem;

	.search {
		display: flex;
		flex-direction: column;
		gap: 1rem;

		.result-article-list {
			margin: 3rem 0;
		}
	}

	.pagination {
	}
`;
